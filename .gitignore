# OMNeT++ / INET build + run artifacts
results/
results-*/
out/
# Phase B: files generated by scripts/gen_topology.py from configs/topology/*.yaml
simulations_gen/
simulations/*.gen.ned
simulations/*.gen.ini
# C1: opt-in pcap(ng) capture output
*.pcap
*.pcapng
*.vec
*.sca
*.vci
*.o
*.so
*_m.cc
*_m.h
Makefile
__pycache__/
*.pyc

# Eclipse/OMNeT++ IDE workspace metadata -- should live outside the repo
# (see run_docker_ide.sh's SYNCSIM_IDE_WORKSPACE), but ignore it here too
# in case it ever ends up inside by accident.
.metadata/
.project
.cproject
.oppbuildspec
